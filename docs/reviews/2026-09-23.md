# 定期レビュー初回記録 — 2026-09-23

この会話で実施した初回レビューの要約。次回の比較基準として保存する。

- HEAD: `fe807943bf914c1cf821a82f0b64e20a098d855c`
- ブランチ: `umineko73/リファクタリング`
- レビュー時の作業ツリー: 変更なし（このレポートと手順書の追加前）
- 比較元: なし
- 対象: App、StickyNoteWindow、StorageService、外部ファイル監視、Markdown描画、関連テスト
- 性能計測: 未実施。以下は静的なコード確認に基づく。

## 指摘と追跡表

| ID | 優先度 | 状態 | 根拠と改善案 |
| --- | --- | --- | --- |
| R001 | 高 | 未解決 | `App.xaml.cs:1298` のSaveAllは全付箋を同期保存し、`App.xaml.cs:933` の新規作成でも呼ばれる。`StorageService.cs:380` は本文とメタデータを毎回保存。変更対象の追跡と付箋単位の失敗処理を導入し、保存処理を独立させる。ファイル単位のアトミック置換はあるが、2ファイル一組の確定ではない。 |
| R002 | 高 | 未解決 | `StickyNoteWindow.Content.cs:201` で外部ファイルの読み込みと表示更新をUIスレッドで行う。読み込みと再試行を独立させ、非同期化時には古い結果の破棄・終了時キャンセル・同時読み込み抑制を組み込む。大容量・遅い保存先での停止時間は未計測。 |
| R003 | 高 | 未解決 | `StickyNoteWindow.xaml.cs:87` 以降に多数の共有状態がある。Windowは20ファイル、計8,749行（コメント・空行を含む）。`StickyNoteWindow.StateTransitions.cs:28` のTransitionToを基点に表示状態と遷移判断を抽出する。フラグを機械的に一つのenumへ置換せず、独立した操作状態を区別する。 |
| R004 | 中 | 未解決 | `App.xaml.cs` は1,438行で起動・付箋管理・トレイ・保存先変更・リマインダー等を担当。`StickyNoteWindow.xaml.cs:50` はApp.Current.Settingsへ依存する。付箋管理の操作と設定を注入可能な単位に分離する。 |
| R005 | 高 | 未解決 | `StorageService.cs:172` は設定読み込み失敗で既定値へ戻り、同`:229` は付箋の読み込み失敗を無通知でスキップする。不在と読み込み失敗を区別した結果を返し、診断・通知・破損設定の退避につなげる。 |
| R006 | 中 | 未解決 | `MarkdownRenderer.cs:46` は構文解析とWPF要素生成を一体で行う。画像・リンク・チェックボックスから位置情報付きの解析結果を抽出する。元テキスト位置と既存構文の互換性を守る。 |

行番号・行数は今回のHEAD時点の参考値。次回は現在のコードで再確認する。

## 既存の土台と移行時の検証

- NoteGeometryStateによる位置管理、StorageServiceの保存先注入、外部更新通知の集約を活用する。
- `NotePersistenceTests` は保存失敗後の再試行、削除後の付箋復活防止を検証している。保存処理の抽出時も維持する。
- 状態遷移・位置・DPIの変更では `NoteTransitionRegressionTests`、`NoteGeometryStateTests`、`NoteScreenPlacementTests` を確認する。
- 外部読み込みでは監視・ログローテーションのテストを維持し、非同期化する段階で結果順序とキャンセルの検証を追加する。
- privateメソッドのリフレクション呼び出しや実時間待ちに依存するテストは、責務の抽出に合わせて移す。既存WPF回帰テストは残す。
- 実際のドラッグ・リサイズ、複数DPIモニタでの操作、性能・メモリは今回未検証。

## テスト結果

実行コマンド：

```powershell
dotnet test tests/ScreenPinNotes.Tests/ScreenPinNotes.Tests.csproj --verbosity minimal
```

- 成功1,136件、失敗0件、スキップ0件。テスト実行時間2分27秒。
- 警告: `StickyNoteWindowTests.cs:5500` のCS8604（Null参照引数の可能性）。
- これは初回レビュー時の実行結果であり、手順書追加時に再実行した結果ではない。

## 次に実施する変更案

1. 保存対象を変更済み付箋へ限定し、保存失敗を付箋単位で扱う。
2. 設定・付箋の読み込み結果へ失敗情報を追加する。
3. 外部ファイル読み込みの責務を抽出し、読み込みの非同期化を別の変更として進める。

状態遷移・App・Markdownの分離は、上記の基盤を整えてから段階的に進める。
